# 2026-04-09 Bug Scan

## Summary

This scan found no new grounded bug candidates in the current recent-change window.

- the only reproduced defect remains the already-tracked Smith MCP SpecKit false-positive bug in
  GitHub `#332` / Linear `MS-127`
- no additional code or workflow defect was grounded from the newer docs-only commits, current MCP
  routing probes, or the smoke-harness unit-test surface

## Scan Window

- previous automation run: `2026-04-08T11:08:32.246Z`
- scan date: `2026-04-09`
- repo basis at scan start: clean `main` at `aae62e5` (`docs: add 2026-04-09 dependency drift audit`)
- recent first-parent window reviewed:
  - `aae62e5` `docs: add 2026-04-09 dependency drift audit`
  - `e51c3bd` `docs: sync tracker docs to packet 031 truth`
  - `b344f1c` `docs(automation): add 2026-04-08 bug scan report`
  - `de338ee` `feat(mcp): expand Smith compatibility surface`
  - `0e81312` `feat(cli): make the session shell a chat-first loop (#325)`
  - `2ef8831` `fix: restrict session fallback and clean coderabbit config`

## Evidence Reviewed

- `git log --oneline --decorate --first-parent -n 8 origin/main`
- `git show --stat --summary --format=fuller aae62e5 e51c3bd b344f1c de338ee`
- `git diff --name-only de338ee..HEAD`
- `git diff 0e81312..de338ee -- crates/mister-smith-mcp/src/compatibility.rs`
- `docs/current-state.md`
- `docs/direction.md`
- `docs/ms_recent_context.md`
- `docs/automation-reports/2026-04-08-bug-scan.md`
- `docs/plans/2026-04-05-smith-mcp-direct-execution-overhaul.md`
- `scripts/live_runtime_proof_smoke.py`
- `scripts/tests/test_live_runtime_proof_smoke.py`
- `WORKFLOW.md`
- `.coderabbit.yaml`
- `.github/ISSUE_TEMPLATE/bug-report.yml`
- `.github/ISSUE_TEMPLATE/workflow-ci-issue.yml`
- current Smith MCP tool responses from:
  - `route_workflow_request`
  - `get_control_plane_snapshot`
  - `prepare_direct_execution(request_text="Inspect recent main history for grounded bugs")`
  - `prepare_direct_execution(request_text="Tighten Smith routing guards without a linked issue")`
- existing issue state:
  - GitHub `#332`
  - Linear `MS-127`
- targeted validation:
  - `cargo test -p mister-smith-mcp route_workflow_request -- --nocapture`
  - `cargo test -p mister-smith-mcp prepare_direct_execution_fails_closed_without_task_identity -- --nocapture`
  - `python3 -m unittest scripts.tests.test_live_runtime_proof_smoke`

## Credible Bug Candidates

No new grounded bug candidates were confirmed in this scan.

The only reproduced defect was the already-tracked `prepare_direct_execution` false SpecKit
detection issue:

- `prepare_direct_execution(request_text="Inspect recent main history for grounded bugs")` still
  returns `should_use_speckit=true` and a `speckit_packet_ready` blocker on current `main`
- the paired controls still show the defect is specific rather than broad:
  - `prepare_direct_execution(request_text="Tighten Smith routing guards without a linked issue")`
    returns `should_use_speckit=false`
  - the current targeted MCP routing tests still pass
- that bug is already filed as GitHub `#332` / Linear `MS-127`, so this run did not open a
  duplicate issue

## Skipped Weak Signals

- `python3 -m unittest scripts.tests.test_live_runtime_proof_smoke` emitted temporary helper
  cleanup `busy` warnings, but the suite still passed (`33` tests, `OK`) and no deterministic
  failure or user-visible regression was reproduced
- `.github/workflows/README.md` is absent even though older guidance still mentions it, but no
  broken GitHub metadata, issue template, or repo issue-plumbing behavior was grounded from that
  absence in this run

## Smallest Safe Fix Directions

- existing tracked fix only: GitHub `#332` / Linear `MS-127`
  - replace substring-based SpecKit detection with an explicit bounded matcher so only true
    packet/spec/task-pack requests opt into SpecKit execution prep
  - add a regression test proving ordinary requests containing `inspect` stay off the SpecKit path
  - keep the existing positive SpecKit routing coverage passing
- no additional safe fix direction is proposed from this scan window because no second grounded
  defect survived review

## Issues Opened

- none
- existing tracked pair retained:
  - GitHub `#332`: <https://github.com/MattMagg/MisterSmith/issues/332>
  - Linear `MS-127`: <https://linear.app/agentic-ops/issue/MS-127>

## Validation Limits

- this run did not perform a fresh live runtime rerun, workspace build, or operator-console
  `npm` validation because the post-`de338ee` scan window contained docs-only commits plus one
  already-tracked MCP defect
- the current conclusions are grounded by recent `main` history, direct Smith MCP reproductions,
  current issue state, and narrow targeted validation only
- report-path closure validation is limited to markdown verification and `git diff --check`
